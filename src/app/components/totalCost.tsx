'use client'

import React, { useEffect } from 'react'

const TotalCost = () => {
    useEffect(()=>{
        
    },[])
    return (
        <div>
        {/* hide it in case of admin login*/}
        {/* allCost will expand as soon as we will click on next button*/}
        {/* after clicking on next button we will show Pay button to complete the transcation and update the items in backend */}
        </div>
    )
}

export default TotalCost
